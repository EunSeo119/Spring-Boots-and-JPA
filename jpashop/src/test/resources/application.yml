spring:
#스프링 부트는 기본적으로 별도의 설정 없으면 메모리 모드로 돌리기 때문에 없어도 됨~
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop  //create에 drop까지 붙여주면 깔끔하게 끝날때 싹 지워줌(drop빼면 처음 실행할땐 지우고 시작하지만 끝날때 지우진 않음)
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace